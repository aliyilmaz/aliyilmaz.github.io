# 🎬 استوديو الترجمة

**استوديو الترجمة** هو تطبيق ويب يتيح لك إضافة ملاحظات فورية إلى مقاطع الفيديو التي تشغلها من كاميرات البث المباشر، أو ملفات الفيديو المحلية، أو يوتيوب، وتحويل هذه الملاحظات إلى ملف ترجمة مُؤرَّخ (.vtt)، وتنزيله كملف مضغوط (ZIP) مع الفيديو. تدعم واجهة التطبيق **التركية، والإنجليزية، والروسية، والعربية، والفرنسية**، ويمكنك إدخال الملاحظات باستخدام الكتابة الصوتية.


---

## ✨ الميزات

- 📹 **ثلاثة مصادر للفيديو**:

- **البث المباشر**: التسجيل من كاميرا الويب، وإيقاف التسجيل مؤقتًا، ومشاهدة الفيديو المسجل.

- **تحميل ملف**: تحديد ملف فيديو من جهاز الكمبيوتر وتشغيله.

- **يوتيوب**: تشغيل فيديوهات يوتيوب بإدخال مُعرِّف الفيديو أو الرابط.

- 🎥 **مُحدِّد الكاميرا**: في تبويب البث المباشر، يمكنك تحديد الكاميرا التي تريد استخدامها قبل بدء التسجيل. لا يمكن تغيير الكاميرا أثناء التسجيل.



**يوتيوب**: تشغيل فيديوهات يوتيوب بإدخال مُعرِّف الفيديو أو الرابط.


**مُحدِّد الكاميرا**: في تبويب البث المباشر، يمكنك تحديد الكاميرا التي تريد استخدامها قبل بدء التسجيل. لا يمكن تغيير الكاميرا أثناء التسجيل. - 📝 **إضافة ملاحظات**:

- أضف ملاحظات إلى أي إطار أثناء التشغيل.

- تُحفظ الملاحظات تلقائيًا مع طابع زمني.

- **الكتابة الصوتية**: (أدخل الملاحظات بالتحدث باستخدام زر الميكروفون).

- 🗂️ **إنشاء ترجمة**:

- تُحوّل الملاحظات المُضافة إلى ملف ترجمة بصيغة `.vtt` (WebVTT).

- يمكنك ضبط مدة عرض كل ملاحظة (من 0.5 إلى 10 ثوانٍ).

- 🎁 **تنزيل ملف ZIP**:

- يُنزّل الفيديو (تسجيل مباشر أو ملف مُحمّل) مع ملف الترجمة كملف ZIP واحد.

- بالنسبة لفيديوهات يوتيوب، يُنزّل ملف الترجمة فقط (لا يُضمّن ملف الفيديو).

- 🌐 **دعم لغات متعددة**: - التركية، الإنجليزية، الروسية، العربية، الفرنسية.

- عند فتح الصفحة، يتم الكشف عن لغة متصفحك تلقائيًا واختيار اللغة المناسبة (الإنجليزية إذا لم تكن مدعومة).


- عند تغيير اللغة، يتم تحديث جميع نصوص واجهة المستخدم ولغة التعرف على الصوت تلقائيًا.

- 📱 **تصميم متجاوب**:

- استخدام مريح على أجهزة الكمبيوتر المكتبية والأجهزة اللوحية والهواتف المحمولة.

- عرض منطقة الفيديو ثابت، بينما عرض منطقة الملاحظات مرن.

---

## 🛠️ التقنيات المستخدمة

- HTML5، CSS3، JavaScript (ES6)
- [JSZip](https://stuk.github.io/jszip/) – لإنشاء ملفات ZIP مضغوطة
- واجهة برمجة تطبيقات YouTube IFrame – لتشغيل فيديوهات YouTube
- واجهة برمجة تطبيقات Web Speech – للكتابة الصوتية (تتطلب دعم المتصفح)
- واجهة برمجة تطبيقات MediaDevices – لاسترجاع قائمة الكاميرات وتحديدها
- Flexbox وCSS Grid – واجهة متجاوبة

---

## 🚀 التثبيت والتشغيل

المشروع يعمل بالكامل على جانب العميل ولا يتطلب أي تثبيت على الخادم.


١. **استنساخ المستودع**:
```bash
git clone https://github.com/aliyilmaz/subtitle-studio.git
```
٢. **تشغيل الملفات على خادم ويب (اختياري):**

يمكنك استخدام خادم HTTP بسيط (مثل `http.server` مع بايثون).

يمكنك أيضًا تشغيله عن طريق فتح ملف `index.html` مباشرةً في المتصفح (مع ذلك، قد تتطلب بعض المتصفحات HTTPS أو localhost لمنح أذونات الكاميرا والميكروفون).

## 🎯 دليل المستخدم
### علامة تبويب البث المباشر
1. حدد الكاميرا التي تريد استخدامها من خلال النقر على زر محدد الكاميرا (📷).
2. ابدأ التسجيل بالضغط على زر "تسجيل".
3. أوقف التسجيل وشاهد الفيديو المسجل.
4. أضف ملاحظات فورية أثناء مشاهدة الفيديو باستخدام الزر "إضافة ملاحظات".
5. أرشفة الفيديو والعنوان الفرعي باستخدام "Finish & ZIP download".

### علامة تبويب رفع الملفات
1. اختر ملف فيديو من جهاز الكمبيوتر.

2. أضف ملاحظات أثناء تشغيل الفيديو.

3. أكمل العملية وقم بتنزيل ملف ZIP.

### علامة تبويب يوتيوب
1. أدخل رابط الفيديو أو معرّفه.

2. أضف ملاحظات أثناء تشغيل الفيديو.

3. قم بتنزيل ملف الترجمة فقط.

### 📝 ملاحظات
+ تعمل ميزة الكتابة الصوتية فقط في المتصفحات التي تدعم واجهة برمجة تطبيقات الكلام على الويب (Web Speech API)، مثل **جوجل كروم، ومايكروسوفت إيدج، وسفاري**.

+ يلزم منح المتصفح إذنًا لاختيار الكاميرا.

+ يتم حفظ الفيديوهات المسجلة بصيغة .webm، والترجمة بصيغة **.vtt**.